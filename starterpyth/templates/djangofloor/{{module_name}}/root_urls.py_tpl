# coding=utf-8

from django.conf.urls import patterns, include, url

urls = [
    ('^example$', '{{ module_name }}.views.index'),
    {% if use_djangorestframework %}url(r'^api-auth/', include('rest_framework.urls', namespace='rest_framework'))
    {% endif %}
]